dePtr bruh = (NodePtr)malloc(sizeof(struct node));
    // bruh->value = 1;

    // head = bruh;
    // tmp = head;
    // for(int i = 0; i < 5; i++) {
    //     bruh->next = (NodePtr)malloc(sizeof(struct node));
    //     bruh = bruh->next;
    //     bruh->value = tmp->value + i;
    //     tmp = tmp->next;
    // }